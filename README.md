# Face-Recognition-Model
A deep learning–based face recognition system built with Python, DeepFace, and FaceNet512. The project detects faces, extracts facial embeddings, compares them against a labeled image database, and identifies the closest match using embedding similarity with confidence estimation and unknown-person detection.
